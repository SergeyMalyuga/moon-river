import { ChangeLanguageDirective } from './change-language.directive';

describe('ChangeLanguageDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeLanguageDirective();
    expect(directive).toBeTruthy();
  });
});
